block viewContainer 
  .col-8 
    each message in message_list
      a#unread(href=message.url value=message.url)
        table.table.table-hover
          tbody
              tr
                td !{message.subject}
                td !{message.subject}
                td !{message.date_formatted}
                

            


        